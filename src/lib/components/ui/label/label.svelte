<script lang="ts">
	import { cn } from '$lib/utils/cn';

	type $$Props = {
		class?: string;
		for?: string;
		children: import('svelte').Snippet;
	};

	let { className = undefined, htmlFor = '', children }: $$Props = $props();
</script>

<label
	for={htmlFor}
	class={cn(
		'text-sm font-medium leading-none peer-disabled:cursor-not-allowed peer-disabled:opacity-70',
		className
	)}
>
	{@render children()}
</label>
